name: Build WiFiGuard

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Install Theos
      run: |
        brew install ldid make xz
        git clone --recursive https://github.com/theos/theos.git $HOME/theos
        echo "THEOS=$HOME/theos" >> $GITHUB_ENV
    
    - name: Install iOS SDK
      run: |
        cd $HOME/theos/sdks
        curl -LO https://github.com/theos/sdks/releases/download/master/iPhoneOS16.5.sdk.tar.xz
        tar -xf iPhoneOS16.5.sdk.tar.xz
        rm iPhoneOS16.5.sdk.tar.xz
    
    - name: Build Package
      run: |
        export THEOS=$HOME/theos
        export PATH="$THEOS/bin:$PATH"
        make package THEOS_PACKAGE_SCHEME=rootless FINALPACKAGE=1 TARGET=iphone:clang:16.5:16.0
    
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: WiFiGuard-deb
        path: packages/*.deb
        retention-days: 30
